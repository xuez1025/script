-- Simple Aimbot UI for Blox Fruits (完整整合版 - 移除右鍵轉向 + 選中玩家綠色高亮 + 無限距離)
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer
local camera = workspace.CurrentCamera

-- UI Creation
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "SimpleAimbotUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = player:WaitForChild("PlayerGui")

local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0, 340, 0, 300)
Frame.Position = UDim2.new(0.5, -170, 0.5, -150)
Frame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
Frame.BorderSizePixel = 0
Frame.Parent = ScreenGui

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Title.Text = "Aimbot"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 20
Title.Parent = Frame

-- 關閉按鈕 X
local CloseButton = Instance.new("TextButton")
CloseButton.Size = UDim2.new(0, 30, 0, 30)
CloseButton.Position = UDim2.new(1, -35, 0, 5)
CloseButton.BackgroundColor3 = Color3.fromRGB(220, 50, 50)
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.Font = Enum.Font.GothamBold
CloseButton.TextSize = 18
CloseButton.Parent = Frame

-- Aimbot 開關按鈕
local ToggleButton = Instance.new("TextButton")
ToggleButton.Size = UDim2.new(0.8, 0, 0, 40)
ToggleButton.Position = UDim2.new(0.1, 0, 0.14, 0)
ToggleButton.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
ToggleButton.Text = "Aimbot: OFF"
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.Font = Enum.Font.Gotham
ToggleButton.TextSize = 16
ToggleButton.Parent = Frame

-- 模式切換按鈕
local ModeButton = Instance.new("TextButton")
ModeButton.Size = UDim2.new(0.8, 0, 0, 35)
ModeButton.Position = UDim2.new(0.1, 0, 0.28, 0)
ModeButton.BackgroundColor3 = Color3.fromRGB(70, 70, 110)
ModeButton.Text = "模式：最近敵人"
ModeButton.TextColor3 = Color3.fromRGB(220, 220, 255)
ModeButton.Font = Enum.Font.Gotham
ModeButton.TextSize = 15
ModeButton.Parent = Frame

-- 狀態標籤
local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(0.8, 0, 0, 30)
StatusLabel.Position = UDim2.new(0.1, 0, 0.42, 0)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "已關閉 (快捷鍵 G)"
StatusLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
StatusLabel.Font = Enum.Font.Gotham
StatusLabel.TextSize = 14
StatusLabel.TextWrapped = true
StatusLabel.Parent = Frame

-- 玩家列表
local PlayerListFrame = Instance.new("ScrollingFrame")
PlayerListFrame.Size = UDim2.new(0.8, 0, 0, 130)
PlayerListFrame.Position = UDim2.new(0.1, 0, 0.55, 0)
PlayerListFrame.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
PlayerListFrame.BorderSizePixel = 0
PlayerListFrame.ScrollBarThickness = 5
PlayerListFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
PlayerListFrame.Parent = Frame

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.Padding = UDim.new(0, 5)
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Parent = PlayerListFrame

-- 拖曳功能
local dragging, dragInput, dragStart, startPos

local function updateDrag(input)
    local delta = input.Position - dragStart
    Frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

Frame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = Frame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

Frame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        updateDrag(input)
    end
end)

-- 核心變數
local SilentAimEnabled = false
local UseClosestMode = true
local SelectedTargetPlayer = nil
local PredictionEnabled = true
local PredictionAmount = 0.135
local maxRange = math.huge          -- 真正無限遠（任何距離的敵人都會被視為「最近」）
local renderConnection
local PlayersPosition = nil
local selectedButton = nil  -- 用來追蹤目前綠色的按鈕

local function getHRP(model)
    return model and model:FindFirstChild("HumanoidRootPart")
end

local function getPredictedPos(hrp)
    if not hrp then return nil end
    if not PredictionEnabled then return hrp.Position end
    local vel = hrp.Velocity
    return hrp.Position + (vel * PredictionAmount)
end

local function getClosestEnemy()
    local lpHRP = getHRP(player.Character)
    if not lpHRP then return nil end
    
    local closest, minDist = nil, math.huge
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= player and plr.Character then
            local hrp = getHRP(plr.Character)
            local hum = plr.Character:FindFirstChildOfClass("Humanoid")
            if hrp and hum and hum.Health > 0 then
                local dist = (hrp.Position - lpHRP.Position).Magnitude
                if dist < minDist then   -- 移除距離上限判斷 → 真正無限遠
                    minDist = dist
                    closest = plr
                end
            end
        end
    end
    return closest
end

-- 更新玩家列表 + 高亮處理
local function updatePlayerList()
    for _, child in ipairs(PlayerListFrame:GetChildren()) do
        if child:IsA("TextButton") then child:Destroy() end
    end
    
    selectedButton = nil  -- 重置高亮
    
    local count = 0
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= player and plr.Character then
            local hrp = getHRP(plr.Character)
            local hum = plr.Character:FindFirstChildOfClass("Humanoid")
            if hrp and hum and hum.Health > 0 then
                local btn = Instance.new("TextButton")
                btn.Size = UDim2.new(1, -10, 0, 30)
                btn.BackgroundColor3 = Color3.fromRGB(65, 65, 65)  -- 預設灰色
                btn.Text = plr.Name .. (plr.Team and " ["..plr.Team.Name.."]" or "")
                btn.TextColor3 = Color3.fromRGB(255, 255, 255)
                btn.Font = Enum.Font.Gotham
                btn.TextSize = 14
                btn.TextXAlignment = Enum.TextXAlignment.Left
                btn.Parent = PlayerListFrame
                
                -- 如果這是目前選中的玩家 → 設為綠色
                if SelectedTargetPlayer == plr then
                    btn.BackgroundColor3 = Color3.fromRGB(0, 180, 0)
                    selectedButton = btn
                end
                
                btn.MouseButton1Click:Connect(function()
                    -- 先把舊的還原灰色
                    if selectedButton then
                        selectedButton.BackgroundColor3 = Color3.fromRGB(65, 65, 65)
                    end
                    
                    SelectedTargetPlayer = plr
                    selectedButton = btn
                    btn.BackgroundColor3 = Color3.fromRGB(0, 180, 0)  -- 綠色高亮
                    
                    StatusLabel.Text = "已選擇： " .. plr.Name .. " (指定模式)"
                    if UseClosestMode then
                        StatusLabel.Text = "已選擇： " .. plr.Name .. " (但目前為最近模式)"
                    end
                end)
                
                count = count + 1
            end
        end
    end
    
    PlayerListFrame.CanvasSize = UDim2.new(0, 0, 0, count * 35)
end

spawn(function()
    while task.wait(5) and ScreenGui.Parent do
        updatePlayerList()
    end
end)

updatePlayerList()

local function startAimbot()
    if renderConnection then return end
    
    renderConnection = RunService.RenderStepped:Connect(function()
        if not SilentAimEnabled then return end
        
        local target = UseClosestMode and getClosestEnemy() or SelectedTargetPlayer
        
        if not target or not target.Character then
            PlayersPosition = nil
            return
        end
        
        local hrp = getHRP(target.Character)
        if not hrp then return end
        
        PlayersPosition = getPredictedPos(hrp)
    end)
end

local function stopAimbot()
    if renderConnection then
        renderConnection:Disconnect()
        renderConnection = nil
    end
    PlayersPosition = nil
end

-- 尋找 Mouse 模組（Blox Fruits 常用方式）
local MouseModule = game:GetService("ReplicatedStorage"):FindFirstChild("Mouse", true)
local Mouse = nil
if MouseModule then
    local success, mod = pcall(require, MouseModule)
    if success and type(mod) == "table" then
        Mouse = mod
    end
end

-- Heartbeat 更新 Mouse.Hit（Silent Aim 核心）
RunService.Heartbeat:Connect(function()
    if SilentAimEnabled and PlayersPosition and Mouse then
        pcall(function()
            Mouse.Hit = CFrame.new(PlayersPosition)
            Mouse.Target = nil
        end)
    end
end)

-- 按鈕事件 - 開關
ToggleButton.MouseButton1Click:Connect(function()
    SilentAimEnabled = not SilentAimEnabled
    if SilentAimEnabled then
        ToggleButton.Text = "Aimbot: ON"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
        local mode = UseClosestMode and "最近敵人" or (SelectedTargetPlayer and SelectedTargetPlayer.Name or "無指定")
        StatusLabel.Text = "已開啟 - 目標：" .. mode
        startAimbot()
    else
        ToggleButton.Text = "Aimbot: OFF"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
        StatusLabel.Text = "已關閉 (快捷鍵 G)"
        stopAimbot()
    end
end)

-- 模式切換
ModeButton.MouseButton1Click:Connect(function()
    UseClosestMode = not UseClosestMode
    if UseClosestMode then
        --SelectedTargetPlayer = nil  -- 可選擇保留已選目標或清除，看你需求
        if selectedButton then
            selectedButton.BackgroundColor3 = Color3.fromRGB(65, 65, 65)
            selectedButton = nil
        end
        ModeButton.Text = "模式：最近敵人"
        ModeButton.BackgroundColor3 = Color3.fromRGB(70, 70, 110)
        if SilentAimEnabled then
            StatusLabel.Text = "切換 → 最近敵人"
        end
    else
        ModeButton.Text = "模式：指定玩家"
        ModeButton.BackgroundColor3 = Color3.fromRGB(110, 70, 130)
        local tgt = SelectedTargetPlayer and SelectedTargetPlayer.Name or "請從列表選擇"
        if SilentAimEnabled then
            StatusLabel.Text = "切換 → 指定模式 (" .. tgt .. ")"
        end
    end
end)

-- 關閉 UI
CloseButton.MouseButton1Click:Connect(function()
    stopAimbot()
    ScreenGui:Destroy()
end)

-- 快捷鍵 G
UserInputService.InputBegan:Connect(function(input, gameProcessedEvent)
    if gameProcessedEvent then return end
    
    if input.KeyCode == Enum.KeyCode.G then
        SilentAimEnabled = not SilentAimEnabled
        if SilentAimEnabled then
            ToggleButton.Text = "Aimbot: ON"
            ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            local mode = UseClosestMode and "最近敵人" or (SelectedTargetPlayer and SelectedTargetPlayer.Name or "無指定")
            StatusLabel.Text = "已開啟 (G鍵) - 目標：" .. mode .. 
            startAimbot()
        else
            ToggleButton.Text = "Aimbot: OFF"
            ToggleButton.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
            StatusLabel.Text = "已關閉 (按 G 再次開啟)"
            stopAimbot()
        end
    end
end)

-- 重生處理
player.CharacterAdded:Connect(function()
    stopAimbot()
    task.wait(0.5)
    updatePlayerList()
end)

print("純 Silent Aimbot UI 已載入 - 無限距離模式 / 選中玩家綠色高亮 / G鍵快速開關")
